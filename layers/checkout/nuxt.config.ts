export default defineNuxtConfig({
  $meta: {
    name: 'checkout',
    description: 'Multi-step checkout flow with address and payment forms',
  },

  components: {
    dirs: [],
  },

  imports: {
    autoImport: false,
  },
})
