export default defineNuxtConfig({
  $meta: {
    name: 'auth',
    description: 'User authentication with JWT, middleware, and protected routes',
  },

  components: {
    dirs: [],
  },

  imports: {
    autoImport: false,
  },
})
